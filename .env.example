# Appwrite 配置（复制此文件为 .env.local 并填写实际值）
VITE_APPWRITE_PROJECT_ID=your_project_id
# DeepSeek（可选：本地演示服务优先用环境变量或 .env.local，缺省自动从 OpenClaw 本地 auth 存储读取；不会提交）
DEEPSEEK_API_KEY=
# 可选：模型名（默认 deepseek-v4-flash）
DEEPSEEK_MODEL=
