# V0.2 启动说明

## Quick Start

### Step 1: Start Frontend

```bash
export PATH="$PWD/.tools/node/bin:$PATH"
npm run dev -- --host 127.0.0.1 --port 4173
```

等待终端输出 Vite ready 后打开 `http://127.0.0.1:4173/`。
